{
    "params": {
        "files": {},
        "config": {}
    },
    "args": {
        "token": "{{ pat }}",
        "sso": "{{ sso }}",
        "provider": "{{ deployment_cloud }}",
        "mode": "org"
    }
}
