{
    "params": {
        "files": {
            "CODEOWNERS":{{ codeowners | tojson }}
        }
    },
    "args": {
        "token": "{{ pat }}",
        "sso": "{{ sso }}",
        "mode": "org"
    }
}